// Retention policy setup modal (checkboxes, custom rule, prune-every, keep-recent).
// Optional per plan; defaults are used if skipped.
// TODO(feat/frontend): implement modal + wire to /session/start.

export function RetentionModal() {
  return null;
}
